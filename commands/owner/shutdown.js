const Discord = require("discord.js");

module.exports = {
    config: {
        name: "shutdown",
    },
    run: async (bot, message, args) => {
        if (message.author.id !== "755714902328606760") return;
    }
}